var toolbox = new grToolbox();

/**
 * This script catch the referrer parameters from the URL and save them in a cookie
 * to be used later on for the grToolbox.processCustomer method on step 3.
 **/
toolbox.catchReferrer();